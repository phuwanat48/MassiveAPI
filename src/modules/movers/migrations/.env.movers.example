# Environment variables required by the movers feature.
# Copy these lines into the shared project .env when merging.
MOVERS_MASSIVE_API_URL=https://massive-api.example.com/v1/prices/latest
MOVERS_MASSIVE_API_KEY=changeme
